// import express from 'express'
// import cache, { ApiCache } from 'apicache'

// export default app = express()


// const cache1week = cache.middleware('1 week')


// app.use((req, res, next) => {
//   console.log('(cache miss)', req.path)
//   next()
// })

// app.get('/some/route', cached, (req, res) => {
//   res.json('')
// })


// cache.on('save', fn)
// cache.after('save', fn)

// cache.get(key, fn)
